<% if @users.any? %>
  <ul class="list-unstyled">
    <% @users.each do |user| %>
      <li>
        <%= link_to gravatar_for(user, size: 50), user %>
        <%= link_to user.name, user %>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>ユーザーはいません</p>
<% end %>
