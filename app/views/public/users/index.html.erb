<h1>Users</h1>
<p>This is the users index page.</p>

<div>
  <%= link_to "ログアウト", destroy_user_session_path, method: :delete %>
</div>


<% if @user_detail.present? %>
  <%= render partial: 'public/users/user_detail', locals: { user: @user_detail } %>
<% end %>

<%= link_to "ユーザー編集画面", my_page_edit_path%>

<%= render partial: 'public/searches/search', locals: { url: search_path } %>


<!--追加記述-->
<% @users.each do |user| %>
  <%= user.membership_id %>
  <%= "#{user.last_name}#{user.first_name}" %>
<% end %>